{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "lint": {},
    "test": {
      "inputs": ["src/**/*.spec.ts", "test/**/*.spec.ts"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
